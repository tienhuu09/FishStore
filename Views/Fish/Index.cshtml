@model FishStore.Models.ViewModels.FishListViewModel
@{
    ViewData["Title"] = "Cá";

}
<div id="categories" class="col-12">
    <vc:place-navigation />

</div>
@foreach (var p in Model.Fishs)
{
    <partial name="FishTemplate" model="p" />
}
<div page-model="@Model.PagingInfo" page-action="Index" page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary" page-url-genre="@Model.CurrentGenre"
     class="btn-group pull-right m-1">
</div>